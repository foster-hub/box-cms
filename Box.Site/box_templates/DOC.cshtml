@inherits Box.CMS.Web.ContentRenderView
@using Box.CMS.Web;
@{
    if (CONTENT != null && CONTENT.File!=null) {
        BoxLib.ResponseFile((string)CONTENT.File.FileUId);    
    }
    else {
        Response.Write("file not found");
    }
    Response.End();
}

